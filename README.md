# このリポジトリについて
スリットを使った時分割パララックスバリアシステムのプログラム 
GLFWで記述しているため実装時にはglfwの環境構築が必要
## 目次
1. [各ブランチの説明](#各ブランチの説明)
2. [環境](#環境)
3. [環境構築](#環境構築)
4. [実行方法](#実行方法)
5. [実行時の最小版](#実行時の最小版)
6. [色補正の切り替えと調整](#色補正の切り替えと調整)
7. [Arduino通信と120Hz同期](#arduino通信と120hz同期)
8. [全キー一覧（詳細）](#全キー一覧詳細)
## 各ブランチの説明
| ブランチ名 | 説明 |
| --- | --- |
| master | VS2017環境でバックライトがLEDのときのプログラム |
| dev | 開発環境
## 環境
1. VS2017  環境  
boost : version 1_73_0  
ターゲットプラットフォーム: x64
## 環境構築
1. [Kinect SDK](https://www.microsoft.com/en-us/download/details.aspx?id=44561)のインストール
2. boostの設定
    - boost1_58_0  
    bullに中のビルド済みboost1_58_0のフォルダをローカルにコピーしてシステム環境変数に**BOOST_DIR**として追加
    - boost1_73_0  
    bullにあるboost1_73_0のzipファイルをローカルに展開してシステム環境変数に**BOOST_DIR_173**として追加
3. Visual Studio
    - 2013  
    bullにあるインストーラとライセンスからインストール
    - 2017  
    [ダウンロードページ](https://visualstudio.microsoft.com/ja/vs/older-downloads/)からVisual Studio Community 2017をインストール  
    ログインアカウントは研究室のMicrosoftアカウント(vmlab_2020@outlook.jp)を使えばよい
4. このリポジトリをクローン
5. 不足しているファイルをコピー  
※.gitignoreによって*.dllはgitの管理下にないのでbullからコピーする必要がある。  
    - bullにあるディレクトリを`(SolutionDir)\Library\`にコピー
    - bullのffmpegの設定フォルダの中のdllファイルを全部`(SolutionDir)/x64/Release`の中にコピー  
    ※`(SolutionDir)/x64/Release`は一度ビルドに失敗すると生成されているはず  
    このとき`Release\x64`（VS 2013なら`Release\WIN32`）で動作させる必要がある
6. 必要な動画ファイルをコピー（ほかのマシンの中を探せば動画ファイルがあるはず）  
※読み込む動画ファイルは*Media.cpp:68*あたりの動画ファイルへのパスから変更できる
## 実行方法
1. Kinect Serverを立ち上げる（Kinect Serverのプログラムは`https://github.com/visual-media-lab/3D_Kinect_Tracking_Server`をクローンするでも良いし、bullからコピーするでもよい。GitHubからのクローンで動作するかは確認していないのでbullからコピーするのが確実。）
2. プログラムを実行
3. 基板のスイッチを切り替えてバックライトを時分割モードに切り替え（この手順はいつでもいい）

## 実行時の最小版
| やりたいこと | キー |
| --- | --- |
| ヘッドトラッキング ON/OFF | `h` |
| 動画モードへ切替 | `m` |
| 静止画モードへ切替 | `Shift + m` |
| 時分割 ON/OFF | `t` |
| 表示内容の切替（モード依存） | `1 / 2` |
| 左右目の描画 ON/OFF | `b / Shift+B`, `v / Shift+v` |
| 幅パラメータ調整 | `[` / `]` |
| バックライト同期ずれの補正 | `k` |
| 色補正 ON/OFF | `n` |

※詳細キーは下の「全キー一覧（詳細）」を参照。

## 色補正の切り替えと調整
- `n` : 色補正（RGBゲイン・ガンマ・クロストーク補償）の ON/OFF を切り替え
- `u/U`, `i/I`, `j/J` : 各色のゲインを増減（小文字で増加、Shift付きで減少）
- `g/G` : ガンマを増減
- `y/Y` : クロストーク補償量を増減

初期値:
- RGBゲイン = `1.00, 1.00, 1.00`
- ガンマ = `1.00`
- クロストーク補償量 = `0.15`
- 色補正 = `ON`

## Arduino通信と120Hz同期
- シリアル設定: `COM1`, `115200bps`
- 起動時: `RESET_SYNC(20)` -> `ENABLE_TIMEDIVISION(12)` を送信
- 実行中（120Hz）: 現在位相に応じて `TIME_DIV_0..3 (200..203)` を1フレームごとに送信
- `t` キー: ON時は `RESET_SYNC -> ENABLE_TIMEDIVISION`、OFF時は `DISABLE_TIMEDIVISION(13)`
- 終了時（Esc）: `DISABLE_TIMEDIVISION(13)` -> `LIGHT_EXIT(110)`

補足:
- PC側は120Hz基準のタイマで位相更新と送信を同じクロックで処理
- V-Syncが使える環境では有効化して描画タイミングを安定化

## 全キー一覧（詳細）
| 入力キー | 動作 |
| --- | --- |
| h | ヘッドトラッキングのON/OFF切り替え |
| m | 動画モードへ移動 |
| Shift + m | 静止画モードへ移行 |
| t | 時分割モードのON/OFF切り替え（本番環境下では時分割モードOFFだと立体視不可）|
| q, w, e, r | フレーム0, 1, 2, 3を描画（本番環境下では使わない） |
| 1 | 静止画モード：静止画1に切り替え, 動画モード：Teapotに切り替え |
| 2 | 静止画モード：静止画2に切り替え, 動画モード：動画表示 |
| 0, 3 ~ 7 | 静止画を切り替え（動画モードでは何も動作しない） |
| Shift + B | 右目の描画OFF |
| b | 右目の描画ON |
| Shift + v | 左目の描画OFF |
| v | 左目の描画ON |
| L / l | LiverModeのON/OFF |
| S | 動画の3DモードON/OFF |
| R | 左右表示順（RL/LR）切り替え |
| f / Shift + f | Zオフセット調整 |
| [ | 幅パラメータのインクリメント |
| ] | 幅パラメータのデクリメント |
| a | 幅パラメータを+1（デバッグ調整） |
| o | 手動シフト（SHIFT）を+1 |
| k | フレームの調整（バックライトとの同期がずれたときに使用) |
| Shift + k | 現在の観測者のz座標と幅パラメータを出力（パラメータ調整用） |
| p / ; | 列ピッチ調整 |
| ' / / | サブピクセルシフト調整 |
| n | 色補正のON/OFF切り替え |
| u / Shift + u | Rゲインの増減 |
| i / Shift + i | Gゲインの増減 |
| j / Shift + j | Bゲインの増減 |
| g / Shift + g | ガンマの増減 |
| y / Shift + y | クロストーク補償量の増減 |
| s（押下中） | 動画更新速度を一時的に変更 |
